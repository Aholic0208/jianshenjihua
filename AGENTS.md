# 后续开发 AI 使用说明

本项目根目录是开发工作区，所有新增内容应按职责放入对应目录，避免把临时文件、代码、素材和文档混在一起。

## 目录用途

- `docs/`：产品文档区。存放 PRD、需求迭代记录、关键决策、会议结论和验收标准。
- `assets/design/`：设计素材区。存放效果图、UI 参考、设计稿导出图等与界面设计直接相关的素材。
- `assets/bug/`：测试问题素材区。存放测试报错截图、异常录屏截图和复现辅助图片。
- `assets/reference/`：参考素材区。存放参考图、灵感收集、竞品截图和非最终设计资料。
- `notes/`：学习笔记区。记录开发中踩过的坑、排查过程、技术方案和经验沉淀。
- `src/`：代码区。所有项目代码都应放在这里。

## 开发规则

1. 所有代码必须放在 `src/` 内，不要在根目录散放业务代码文件。
2. `src/` 内的代码不应直接依赖 `docs/`、`notes/` 或 `assets/` 中的外部说明文件运行；运行所需资源应放入代码区内由代码明确管理。
3. 产品需求、设计决策、验收口径和迭代记录统一放入 `docs/`。
4. 开发过程中的技术笔记、问题复盘和临时学习记录统一放入 `notes/`。
5. 图片和设计素材按用途分别放入 `assets/design/`、`assets/bug/`、`assets/reference/`，不要混放。
6. 修改或新增功能前，先查看 `docs/` 中的需求和关键决策，再查看 `notes/` 中是否已有相关经验记录。
7. 若新增目录，应更新本文件，说明目录用途和使用边界。
8. 每次完成阶段性开发或文档更新后，应将变更提交到 Git，并上传到项目对应的 GitHub 仓库。提交前先检查 `git status`，确认没有误提交临时文件、敏感信息或无关素材；推送前确认远端地址正确。
9. 如果当前仓库还没有配置 GitHub remote，应先向用户索要仓库地址，不要自行猜测或创建远端。
10. 禁止批量删除文件或目录。不要使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。需要删除文件时，只能一次删除一个明确路径的文件，例如 `Remove-Item "C:\path\to\file.txt"`。如果需要批量删除文件，应停止操作，并请用户手动删除。
